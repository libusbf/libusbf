SUBDIRS = src
ACLOCAL_AMFLAGS = -I m4
library_includedir=$(includedir)
library_include_HEADERS = include/libusbf.h

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = libusbf.pc

EXTRA_DIST = libusbf.pc.in
DISTCLEANFILES = libusbf.pc
